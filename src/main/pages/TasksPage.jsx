import { useContext } from "react"
import { UserContext } from "../../context/userContext";

export const TasksPage = () => {

    return(
        <>
            <h1>Tasks</h1>
            
        </>
    )
}